( function() {

    if ( window ) {

        if ( window.ppconsole === undefined ) {
            
            window.ppconsole = {
                
                /**
                 * major.minor.status.revision
                 */
                version : '0.0.0.1'
                
            };
            
        }

    }
    
} )();
